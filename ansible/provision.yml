---
- name: Apply common configuration
  hosts: all
  remote_user: root
  roles:
    - common
    - deployer_user
    - install_monit
    - git_checkout

- name: Apply bitcoin node configuration
  hosts:
    - nodes
    - dev
  remote_user: root
  roles:
    - node
